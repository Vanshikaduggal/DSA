class Solution {
public:
    bool isPowerOfFour(int n) {
        int x=100;
        for(int i=0;i<x;i++)
        {
            if(pow(4,i)==n)
            return true;
        }
        return false;
    }
};
