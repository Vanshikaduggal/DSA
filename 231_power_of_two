class Solution {
public:
    bool isPowerOfTwo(int n) {
        int x=100;
        for(int i=0;i<x;i++)
        {
            if(pow(2,i)==n)
            {
                return true;
            }
        }

        return false;
    }
};
